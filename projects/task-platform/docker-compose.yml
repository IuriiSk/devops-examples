version: "3.9"

services:
  redis:
    image: redis:7-alpine

  api:
    build: ./api
    ports:
      - "5000:5000"
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis

  worker:
    build: ./worker
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis