# Flask Demo App for DevOps

Демонстрационное приложение для проверки навыков DevOps.

## Локальный запуск
```bash
export REDIS_HOST=localhost DB_HOST=localhost
pip install -r requirements.txt
python app.py